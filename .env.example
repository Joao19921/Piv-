NODE_ENV=production
PORT=3000

# Habilita Basic Auth no ambiente publicado quando ambos estiverem preenchidos.
TEST_ACCESS_USER=pivo-teste
TEST_ACCESS_PASSWORD=troque-esta-senha

# Opcional: conector externo para benchmark salarial ao vivo.
MARKET_BENCHMARK_CONNECTOR_URL=

# Postgres (Supabase). Sem esta variavel, o app roda 100% com os snapshots estaticos
# embutidos no codigo (comportamento anterior). Pegue a connection string em:
# Supabase > Project Settings > Database > Connection string (modo "Direct connection").
DATABASE_URL=postgresql://postgres:SENHA@db.SEU-PROJETO.supabase.co:5432/postgres

# AWS Pricing API (GetProducts). Credencial IAM somente leitura: crie um usuario com a
# policy gerenciada "AWSPriceListServiceFullAccess" ou uma policy inline com apenas
# pricing:GetProducts e pricing:DescribeServices (nao concede acesso a nenhum recurso da conta).
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# GCP Cloud Billing Catalog API. Crie uma API key no Google Cloud Console restrita
# a "Cloud Billing API" (nao precisa de service account nem de projeto de billing vinculado).
GOOGLE_CLOUD_BILLING_API_KEY=
