[project]
name = "benchmark-worker"
version = "0.1.0"
description = "Worker independente para coleta, normalizacao e persistencia de referencias salariais de mercado"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
